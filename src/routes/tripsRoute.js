import express from 'express';

const router = express.Router();

// router.post('/', console.log("create new trip"));
// router.put('/:tripId/:action', console.log("complete a trip"));
// router.get('/:active', console.log("get active trip"));

export default router;
